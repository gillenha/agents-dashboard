{
  "name": "devpigh",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "concurrently --names api,dashboard --prefix-colors cyan,magenta \"pnpm --filter api dev\" \"pnpm --filter dashboard dev\"",
    "build": "pnpm --filter shared build && pnpm --filter api build && pnpm --filter dashboard build",
    "typecheck": "pnpm -r typecheck",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:init": "pnpm --filter api db:init",
    "db:reset": "docker compose down -v && docker compose up -d && sleep 2 && pnpm db:init"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}

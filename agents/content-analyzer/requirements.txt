# Direct dependencies for this agent.
# devpigh-agent SDK is installed separately:
#   local dev: pip install -e ../../packages/agent-sdk
#   Docker:    see Dockerfile (COPY + pip install /tmp/agent-sdk)
anthropic>=0.39.0
